{
  "name": "oficin-ia-checklist-v15-repo-separado",
  "version": "15.0.0",
  "private": true,
  "description": "OFICIN-IA Checklist separado do SaaS, mesmo Firebase e APK próprio.",
  "scripts": {
    "prepare:apk": "node scripts/prepare-apk.js",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "build:android": "npm run prepare:apk && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/assets": "latest",
    "@capacitor/cli": "latest"
  }
}
